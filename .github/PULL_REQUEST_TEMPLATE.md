> ⚠️ **This is a public repository mirroring code from Synorb's private monorepo.**
> No pull request here is merged automatically. It requires the founder's
> explicit review and **two separate confirmations** before merge, no matter
> how small the change looks — including dependency-update PRs opened by
> Dependabot. If you are the founder reviewing this: read the full diff
> yourself before approving, treat any change to `AGENTS.md`, `.cursorrules`,
> `.codex-plugin/`, `.well-known/`, or `.github/` with extra suspicion (these
> are AI-agent instruction files and a common target for a malicious PR to
> quietly poison), and confirm twice — once that you've reviewed it, once
> that you intend to merge it.

## What changed

## Why

## How this was tested
